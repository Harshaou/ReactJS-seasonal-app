/*const text = season === 'winter' ? 'Burr its chilly': 'burr lets hit the beach';
const icon = season === 'winter' ? 'snowflake' : 'sun'; */


    /*   return (
          <div>
          <i className={`${icon} icon`} />
              <h1>{text}</h1>
          <i className={`${icon} icon`} />
          </div>
      )
*/